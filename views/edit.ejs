<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit </title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
  </head>
  <body>
    <div class="container">
        <h1>Edit</h1>
        <form method="post" action="/build/update/<%= manage._id %>">
          <div class="form-group">
            <label for="item">ชื่อตึก(ย่อ)</label>
            <input type="text" class="form-control" id="name" name="name" value="<%= manage.name %>">
          </div>
          <div class="form-group">
            <label for="item">ชื่อตึก(เต็ม)</label>
            <input type="text" class="form-control" id="namefull" name="namefull" value="<%= manage.namefull %>">
          </div>
          <button type="submit" class="btn btn-default">Update</button> 
          <!-- button update -->
        </form>
    </div>
  </body>
</html>